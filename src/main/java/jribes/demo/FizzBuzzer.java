package jribes.demo;

public class FizzBuzzer {
    public String fizzbuzz(int number) {
        if (number == 3) {
            return "fizz";
        }
        return String.valueOf(number);
    }
}
